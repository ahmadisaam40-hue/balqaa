rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary open rules for fast launch. Replace with stricter auth rules later.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
